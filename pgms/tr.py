import textwrap
def trail(a,b):
    c=dict()
    e=textwrap.wrap(a,1)
    for i in range(len(b)):
        c[a[i]]=0
        d[b[i]]=0           
    for i in range(len(b)):
        c[a[i]]+=1
        d[b[i]]+=1
    for i in range(len(b),len(a)):
        for j in range(100):
            if c[i]
    
        
a=input()
b=input()
trail(a,b)
